[project]
name = "flow-state-tests"
version = "0.1.0"
description = "Tests for flow-state skills and MCP servers"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24.0",
    "aiohttp>=3.9",
    "imap-slim-mcp",
    "obsidian-slim-mcp",
]

[tool.uv.sources]
imap-slim-mcp = { path = "../imap-slim-mcp" }
obsidian-slim-mcp = { path = "../obsidian-slim-mcp" }

[tool.pytest.ini_options]
testpaths = [
    "youtube-to-markdown",
    "imap-slim-mcp",
    "obsidian-slim-mcp",
    "chrome-control",
]
pythonpath = [
    "../youtube-to-markdown",
    "../imap-slim-mcp",
    "../obsidian-slim-mcp",
    "../chrome-control",
]
python_files = "test_*.py"
python_functions = "test_*"
